gnome = import ('gnome')

project_resources = gnome.compile_resources (
    meson.project_name (),
    files (meson.project_name () + '.gresource.xml'),
    c_name: 'prject_resources',
    source_dir: ['.'],
)